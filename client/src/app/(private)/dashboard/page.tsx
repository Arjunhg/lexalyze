"use client";

import UserContracts from "@/components/dashboard/user-contracts";

// render upload model so user can uoload their contract in the model
export default function Dashboard(){

    return (
        <div>
            
            <UserContracts/>
        </div>
    )
}